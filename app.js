document.addEventListener('DOMContentLoaded', (event) => {
    // Simulate a delay (you might want to replace this with actual loading logic)
    setTimeout(() => {
        document.body.classList.add('loaded');
    }, 2000); // 2000 milliseconds (adjust as needed)
});